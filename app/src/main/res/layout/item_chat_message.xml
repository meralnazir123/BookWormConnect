<?xml version="1.0" encoding="utf-8"?>
<!--
    item_chat_message.xml — single chat bubble row
    rowContainer  = full-width row, gravity flipped per sender
    bubbleContainer = the coloured pill bubble
    tvTag    = small label ("❓ Question" / "💬 Answer") — can be GONE
    tvMessage = message text
    tvTime    = timestamp (hh:mm a)
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rowContainer"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:paddingStart="8dp"
    android:paddingEnd="8dp"
    android:paddingTop="4dp"
    android:paddingBottom="4dp">

    <!-- Bubble wrapper (max 75% width) -->
    <LinearLayout
        android:id="@+id/bubbleContainer"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="10dp"
        android:maxWidth="290dp"
        android:background="@drawable/bg_bubble_received">

        <!-- Tag label (Question / Answer) -->
        <TextView
            android:id="@+id/tvTag"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="10sp"
            android:textStyle="italic"
            android:textColor="#AA000000"
            android:paddingBottom="2dp"
            android:visibility="gone"/>

        <!-- Main message text -->
        <TextView
            android:id="@+id/tvMessage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="14sp"
            android:textColor="@color/textPrimary"
            android:lineSpacingMultiplier="1.2"/>

        <!-- Timestamp -->
        <TextView
            android:id="@+id/tvTime"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="end"
            android:paddingTop="3dp"
            android:textSize="10sp"
            android:textColor="#99000000"/>

    </LinearLayout>

</LinearLayout>